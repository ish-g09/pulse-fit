version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: pulsefit_postgres
    environment:
      POSTGRES_DB: pulsefit_db
      POSTGRES_USER: pulsefit
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: